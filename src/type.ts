export type intialStateProps = {
  balance: number;
  loan: number;
  loanPurpose: string;
};
export type actionProps = {
  type: string;
  payload?: any;
};
export type intialStateCustomerProps = {
  fullName: string;
  nationalId: string;
  createAt: string;
};
